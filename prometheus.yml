global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'payship-core'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:8080']
        labels:
          env: 'local'
          service: 'orders'